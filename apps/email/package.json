{
  "name": "@nextorders/email",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs",
    "clean": "rm -rf dist",
    "clean:modules": "rm -rf .nuxt node_modules",
    "typecheck": "nitro prepare",
    "postinstall": "nitro prepare"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "catalog:",
    "@vue-email/components": "catalog:",
    "@vue-email/render": "catalog:",
    "nodemailer": "catalog:"
  },
  "devDependencies": {
    "@types/nodemailer": "catalog:",
    "nitropack": "latest"
  }
}
